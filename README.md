# React Luau Props Helper

VS Code extension that adds context-aware IntelliSense for React-Luau `createElement` calls.

When you type:

```lua
local React = require(ReplicatedStorage.Packages.React)
local e = React.createElement

e("TextLabel", {
    -- here
})
```
